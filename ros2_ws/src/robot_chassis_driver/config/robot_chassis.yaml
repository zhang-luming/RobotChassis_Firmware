# ==============================================================================
# RobotChassis 底盘驱动配置文件
# ==============================================================================

/**:
  ros__parameters:

    # ==========================================================================
    # 一、硬件配置
    # ==========================================================================

    # 串口设备
    serial_port: "/dev/ttyUSB0"

    # 串口波特率（需要与MCU固件一致）
    serial_baudrate: 921600

    # ==========================================================================
    # 二、时间同步配置
    # ==========================================================================

    # PTP同步间隔（毫秒）
    ptp_interval_ms: 200.0

    # ==========================================================================
    # 三、坐标系配置
    # ==========================================================================

    # 里程计坐标系
    odom_frame_id: "odom"

    # 机器人基础坐标系
    base_frame_id: "base_link"

    # IMU传感器坐标系
    imu_frame_id: "imu_link"

    # ==========================================================================
    # 四、里程计校准系数
    # ==========================================================================

    # X方向速度校准系数
    # 用途：修正轮速里程计的线速度测量误差
    # 建议：通过直行测试标定（实际距离/测量距离）
    odom_x_scale: 1.0

    # Yaw角速度校准系数
    # 用途：修正轮速里程计的角速度测量误差
    # 建议：通过原地旋转测试标定（实际角度/测量角度）
    odom_yaw_scale: 1.0

    # ==========================================================================
    # 五、机械参数（需要根据实际底盘测量调整）
    # ==========================================================================

    # 轮距：左右轮中心距（米）
    # 测量方法：测量左轮中心到右轮中心的水平距离
    wheelbase: 0.5

    # 轮半径（米）
    # 用于计算线速度与角速度的转换
    wheel_radius: 0.1

    # 编码器每米脉冲数
    # 计算公式：ticks_per_meter = 编码器分辨率 / (2 * π * wheel_radius)
    # 例：1000线编码器，轮半径0.1m → 1000 / (2 * π * 0.1) ≈ 1591.5
    ticks_per_meter: 1000.0

    # ==========================================================================
    # 六、IMU数据配置
    # ==========================================================================

    # IMU数据单位转换（由固件确定，通常不需要修改）
    # 欧拉角：0.01度 → 弧度
    # 陀螺仪：0.01弧度/s → 弧度/s
    # 加速度：0.01g → m/s²
